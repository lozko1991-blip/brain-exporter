# Секрети — НІКОЛИ не комітити
.env
.env.*
secrets.json
secrets.local.json
*.secret

# Кеш товарів (великий, живе у GitHub Release, не в git)
products_cache.json
products_cache.shard*.json
# SID сесії Brain (передається між джобами, ніколи не комітити)
.brain_sid
# Згенеровані XML-фіди — віддаються через GitHub Release (можуть бути >100МБ)
output/*.xml

# Python
__pycache__/
*.pyc
.venv/
venv/

# Системне
.DS_Store
Thumbs.db

# Бекапи коду перед правками
backups/
